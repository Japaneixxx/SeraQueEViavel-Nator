# Senha do painel do gestor. Sem isso configurado, /admin não funciona.
ADMIN_PASSWORD=troque-esta-senha

# Segredo usado para assinar o cookie de sessão do painel do gestor.
# Gere um valor aleatório, ex: openssl rand -hex 32
ADMIN_SESSION_SECRET=troque-este-segredo
